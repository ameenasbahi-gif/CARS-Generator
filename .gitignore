# Environment variables — never commit these
backend/.env
.env

# MCP config contains access tokens — keep local only
.mcp.json

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/

# SQLite (old, replaced by Supabase)
*.db

# Local start script
start.sh
